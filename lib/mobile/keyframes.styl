@keyframes mobile__qr-image
    100%
        border-radius 0
        opacity 1
        transform scale(1)
        visibility visible

@keyframes mobile--active
    100%
        opacity 1

@keyframes mobile__label
    100%
        opacity 1
        visibility visible
        transform rotate(0)
